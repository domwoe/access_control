service : {
    "update_permissions": (principal, text, bool) -> (text);
    "read_permissions_certified": ()              -> (opt blob) query;
    "verify_permissions": (principal, text)       -> (bool) query;
}
